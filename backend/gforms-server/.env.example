# Google Forms API Configuration
# Get these from Google Cloud Console: https://console.cloud.google.com
# Enable the Google Forms API: https://console.cloud.google.com/apis/library/forms.googleapis.com

# OAuth 2.0 Client ID
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# OAuth 2.0 Client Secret
GOOGLE_CLIENT_SECRET=your-client-secret

# OAuth 2.0 Redirect URI
# For local testing: http://localhost:3000/oauth/callback
# For AWS: https://your-alb-dns/oauth/callback
GOOGLE_REDIRECT_URI=http://localhost:3000/oauth/callback

# Path to store OAuth tokens (STDIO mode only)
TOKEN_PATH=./token.json

# Path to store credentials (STDIO mode only)
CREDENTIALS_PATH=./credentials.json

# HTTP Server Configuration
# Port for HTTP server
PORT=3000

# Node environment (development, production)
NODE_ENV=development

# Redis Configuration (required for HTTP mode)
# For local testing: redis://localhost:6379
# For AWS: redis://your-elasticache-endpoint:6379
REDIS_URL=redis://localhost:6379

# Session Configuration
SESSION_SECRET=your-random-secret-key-change-in-production
SESSION_MAX_AGE=86400000

# Cookie Configuration (Production)
# Optional: Set cookie domain for production (e.g., .daffyos.in for subdomains)
# Leave blank for localhost development
COOKIE_DOMAIN=

# Encryption Configuration (required for token storage)
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=your-64-character-hex-encryption-key
